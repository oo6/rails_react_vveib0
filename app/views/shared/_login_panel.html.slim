.login-panel.panel.panel-info
  .panel-heading
    h3.title 登录
  .panel-body
    = form_for :session, url: login_path do |f|
      .form-group
        = f.label :email, '邮箱'
        = f.text_field :email, class: 'form-control', tabindex: 1
      .form-group
        = f.label :passowrd, '密码'
        |  (
        a.forget-password href=new_password_reset_path 忘记密码
        | )
        = f.password_field :password, class: 'form-control', tabindex: 2
      .checkbox
        = f.label :remember_me, class: "checkbox inline" do
          = f.check_box :remember_me
          span 记住我

      = f.submit "登录", class: "btn btn-success", tabindex: 3

      a.btn.btn-primary.pull-right href='/auth/github' Login via GitHub
